#!/bin/bash
source ~/.cache/wal/colors.sh

ad=$(acpi -a | grep -wo "on-line\|off-line")
bp=$(acpi -b | grep "Battery 0" | grep -o '[0-9]\+%' | tr -d '%')
be="$( printf '%x\n' $bp )"

if [[ $bp -ge "80" ]]; then be='#'
fi
if [[ $ad = "on-line" ]]; then ch='#'
fi

echo "^c$color10^$ch$be"
